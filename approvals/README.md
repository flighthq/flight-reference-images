# Approved candidates

Each JSON file is an independently reviewed, immutable candidate approval. Approval PRs add only their own file, so they can merge in any order. Release staging consumes approved candidates and records their request ids in the next manifest.
