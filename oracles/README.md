# Oracle records

The path `oracles/<subject>/<entry>/<renderer>.json` is the stable identity of one blessed reference image. Git stores only this record. The encoded PNG lives at the matching `images/<subject>/<entry>/<renderer>.png` path inside the release pack named by the record.
